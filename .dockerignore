# VCS
.git
.gitignore
.github

# Fichiers Docker non nécessaires dans l'image (mais docker/nginx/ est gardé pour le COPY)
docker/php/
docker/compose*.yaml
docker/.env*

# Dépendances (gérées dans l'image via composer)
vendor/

# Cache / logs Symfony (générés à runtime)
var/

# Tests
tests/
phpunit.dist.xml

# OS
.DS_Store
Thumbs.db

# Fichiers de conf locaux (secrets — injectés via Docker env_file)
.env.local
.env.*.local

# Docs
*.md
PLAN.md
